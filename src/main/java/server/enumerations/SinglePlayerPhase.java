package server.enumerations;

import server.Model.Player;

/**
 * This enum contains all possible States of a {@link Player Player}.
 */
public enum SinglePlayerPhase {
        START, PICK_FROM_LIVINGROOM, PUT_IN_SHELF, END
}
